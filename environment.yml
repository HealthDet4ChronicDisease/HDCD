name: hdcd_env
channels:
  - conda-forge
  - defaults
dependencies:
  - conda
  - python
  - matplotlib
  - vega_datasets
  - pip
  - pip:
      - altair==5.1.2
      - numpy
      - pandas
      - scipy
      - geopandas
prefix: /Users/suxian/miniconda3
